CREATE TABLE t_estudiantes(
    cod_estudiante VARCHAR(10) NOT NULL PRIMARY KEY,
    nom_estudiante VARCHAR(100) NOT NULL ,
    ape_estudiante VARCHAR(100) NOT NULL ,
    con_estudiante VARCHAR(100) NULL
);